require "spec_helper"

describe WarsawViewTool do
  it "has a version number" do
    expect(WarsawViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
